{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Just change the Server and Update-Database command on Package Manager Terminal,
    // And everything should work fine if we need to change the Database.
    "DefaultConnection": "Server=.;Database=Nerxeas;Trusted_Connection=True;TrustServerCertificate=True"
  }
}